# VoiceOS — Blockers

## FASE 1 — `git merge origin/main` (branding/white-label lineage)

**Status:** BLOCKED — needs human-guided merge with visual QA.

**What was attempted:** `git merge origin/main` into `claude/voiceos-saas-build-D8nrY`.
Merge-base is `43183f7`; the two lines diverged into **different product features
that touch the same files**:

- **This branch:** QAC-advanced voice-agent platform (compliance nav, provider
  routers/circuit breakers, the Groq migration, FASE 0 security hardening).
- **origin/main (15 commits):** white-label branding / dynamic color theming,
  latency optimization, logo upload, admin Plan selector.

**11 conflicts.** Resolved safely and confirmed:

| File                   | Resolution                                                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `middleware.ts`        | Took HEAD — **preserves FASE 0 security** (does NOT let main re-introduce `/api/debug`, `/api/auth/direct-access`, or the `x-debug-auth-reason` header leak). The `/api/cron` PUBLIC_PATHS fix from main is already present on the branch. |
| `agent/worker_core.ts` | 28 conflict blocks. Took the branch (advanced, covered by 450 passing tests). **main's latency tweaks to the worker are NOT merged** — they must be re-applied and validated with live calls.                                              |
| `dist/worker.mjs`      | Build artifact — took branch (regenerated by `build:worker`). Should be git-ignored (tracked separately).                                                                                                                                  |
| `pnpm-lock.yaml`       | Regenerate via `pnpm install` after `package.json` (which auto-merged).                                                                                                                                                                    |

**The actual blocker — branding vs QAC UI render logic** (5 files):
`components/layout/sidebar.tsx`, `components/layout/header.tsx`,
`app/admin/workspaces/WorkspaceCommandCenter.tsx`,
`app/api/admin/workspaces/[id]/plan/route.ts`, `lib/cartesia.ts`.

These files diverged into **different rendering logic**: main adds brand-color
theming (`primaryColor` accent bar, brand-colored active nav states, logo), while
the branch added QAC/compliance nav items, `sub` nav entries, `hasDesignAccess`,
and `isQaAdmin` gating. A prop-union reconciliation makes `tsc`/`build` pass, but
**does not guarantee both features render correctly** — silently dropping either
the white-label branding or the QAC nav (the stated commercial differentiator)
would still build green. This can only be validated by running the app and
eyeballing the sidebar/header/admin UI, which is not possible in this headless
environment.

**Decision (reversible, per protocol):** `git merge --abort`. The branch remains
clean, building, and test-green at `fbdd928`. The layout prop-union approach is
proven out (see `app/(app)/layout.tsx` merge, which was clean), so the remaining
work is bounded.

**Recommended resolution (human, ~1–2h with the app running):**

1. Re-run `git merge origin/main`.
2. `middleware.ts` → keep HEAD (security). `worker_core.ts` → keep branch;
   file a follow-up to port main's latency tweaks with a live-call test.
3. For the 5 UI files: merge the **prop union** (branding + QAC), then run the
   app and confirm BOTH the white-label branding AND the QAC/compliance nav
   render correctly for a QAC-enabled workspace and a branded workspace.
4. `pnpm install` to regenerate the lockfile; `pnpm build && pnpm test`.

**Impact of not merging now:** none to the branch's own functionality. The branch
does not depend on main's branding to operate; the later phases (dialer, Retell
removal, provider cleanup) are independent of this merge.
