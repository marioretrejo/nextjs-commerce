import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Agent' };

export default function AgentDetailPage() {
  return <h1 className="text-2xl font-bold">Agent Detail — coming in Step 7.</h1>;
}
