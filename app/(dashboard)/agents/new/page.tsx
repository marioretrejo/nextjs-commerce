import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'New Agent' };

export default function NewAgentPage() {
  return <h1 className="text-2xl font-bold">Agent Builder — coming in Step 7.</h1>;
}
