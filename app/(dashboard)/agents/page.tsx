import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Agents' };

export default function AgentsPage() {
  return <h1 className="text-2xl font-bold">Agents — coming in Step 6.</h1>;
}
