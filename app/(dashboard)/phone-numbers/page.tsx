import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Phone Numbers' };

export default function PhoneNumbersPage() {
  return <h1 className="text-2xl font-bold">Phone Numbers — coming in Step 9.</h1>;
}
