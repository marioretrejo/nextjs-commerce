import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Call Logs' };

export default function CallsPage() {
  return <h1 className="text-2xl font-bold">Call Logs — coming in Step 8.</h1>;
}
