import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Call Transcript' };

export default function CallDetailPage() {
  return <h1 className="text-2xl font-bold">Call Transcript — coming in Step 8.</h1>;
}
