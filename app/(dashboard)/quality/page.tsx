import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'QA Engine' };

export default function QualityPage() {
  return <h1 className="text-2xl font-bold">QA Engine — coming in Step 10.</h1>;
}
