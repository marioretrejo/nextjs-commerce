import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Billing' };

export default function BillingPage() {
  return <h1 className="text-2xl font-bold">Billing — coming in Step 11.</h1>;
}
