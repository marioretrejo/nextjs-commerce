import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Campaigns' };

export default function CampaignsPage() {
  return <h1 className="text-2xl font-bold">Campaigns — coming in Step 9.</h1>;
}
