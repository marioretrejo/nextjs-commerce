// Ambient stub — @livekit/rtc-node is a native addon used only by agent/worker_core.ts,
// which is excluded from the Next.js TypeScript compilation via tsconfig.json.
// This stub prevents Next.js 15's type-checker from failing if it reaches the
// agent directory through nested-tsconfig detection.
declare module '@livekit/rtc-node';
